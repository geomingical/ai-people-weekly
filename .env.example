# Copy to .env and fill in. .env is gitignored and must never be committed,
# printed, or pasted into a run report.
#
# Names are case-sensitive: GROQ_API_KEY, not Groq_API_KEY.
NVIDIA_API_KEY=
GROQ_API_KEY=
