# macOS
.DS_Store

# secrets — never commit
.env
.env.*
!.env.example

# dependencies and build output
node_modules/
dist/
.astro/

# test output
test-results/
playwright-report/
coverage/

# local preview
.preview/

# isolated feature worktrees
.worktrees/

# local AI assistant instructions
AGENTS.md
CLAUDE.md

# Run state. Regenerated by the pipeline; committing it would make one
# machine's view of "what the feed held last week" everyone else's.
pipeline/state/
